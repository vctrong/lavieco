# Public site origin (used for metadata, Open Graph and canonical URLs).
# NEXT_PUBLIC_* values are embedded in the bundle at build time: never put secrets here.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
